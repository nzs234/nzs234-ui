{
  "key": "anima_depth_expansion_enabled",
  "title": "Anima 模型扩层（Depth Up-Scaling）",
  "category": "Anima / 高级",
  "appliesTo": [
    "anima-finetune"
  ],
  "layers": [
    "模型结构层"
  ],
  "standard": {
    "summary": "训练加载时把 Anima DiT 交错复制相邻层扩到更多层数，新层的输出投影归零（恒等残差），得到一个更深的可训练底座。仅支持 Anima 全参微调。",
    "effect": "模型按 anima_depth_expansion_target_layers 重建为更深结构（如 28 层 → 40 层），初始行为与原模型一致（新层零初始化输出）；训练范围由 anima_depth_expansion_train_scope 控制；保存的产物是完整扩层底座。",
    "whenToUse": "想以较低成本扩大模型容量（Depth Up-Scaling 路线）、在原模型能力基础上继续拉伸上限时；准备做扩层后长期微调的项目。",
    "avoidWhen": "LoRA 训练不支持；显存按扩层后的层数计费，16GB 级显卡谨慎评估；从扩层 checkpoint 恢复训练时必须保持相同的扩层配置（层数不匹配后端会明确报错）。"
  },
  "advanced": {
    "principle": "Depth Up-Scaling（如 SOLAR 的做法）：交错复制已有 transformer 层构造更深网络，把新层的输出投影零初始化使残差路径退化为恒等，保证扩层瞬间模型输出不变，训练从「无损起点」开始学习新增容量。",
    "intervention": "config_adapter_conversion_context.py：归一化 enabled/target_layers/train_scope（scope 非法且 enabled 时直接抛错）；anima_depth_expansion_runtime.py：加载时执行交错复制与零投影；anima_full_finetune.py：resume 时校验 checkpoint 层数与配置一致，不一致给出修复指引。",
    "expectedImpact": "参数量与显存按层数比例上升；扩层容量需要足够数据与步数才能兑现为质量收益。",
    "tradeoffs": "产物是完整底座（体积大，非 LoRA）；扩层模型的推理成本永久上升；训练范围选 all 时成本最高、选 new_layers 最接近标准 DUS 配方。",
    "codePath": "anima_depth_expansion_runtime.py：扩层执行；config_adapter_conversion_context.py：配置归一化；anima_full_finetune.py：resume 层数校验"
  },
  "relatedConfigs": [
    "anima_depth_expansion_target_layers",
    "anima_depth_expansion_train_scope",
    "model_train_type"
  ]
}
