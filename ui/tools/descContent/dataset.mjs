// SPDX-License-Identifier: LicenseRef-PolyFormNoncommercial-1.0.0
// desc 文案表 —— 数据集：分辨率/分桶、caption、增强、缓存、先验保留。
// 推荐范围依据：training_wiki（resolution/network_dim/keep_tokens/shuffle_caption/noise_offset 等条目）、
// backend configs_dataset.py 默认值与 training_config_checks.py 校验域。
export default {
  // ── 路径 ──
  train_data_dir: ['训练数据集根目录：每个子文件夹是一个概念，文件夹名 = 重复次数@概念名（如 10_lulu）。建议图片统一存放于此盘，SSD 更快。', 'Dataset root: each subfolder is one concept named repeats@concept (e.g. 10_lulu). Prefer an SSD for speed.'],
  reg_data_dir: ['先验保留（prior preservation）的正则图目录，配合 prior_loss_weight 使用。建议仅在防灾难遗忘需求下提供类别图。', 'Regularization-image folder for prior preservation with prior_loss_weight. Provide class images only when fighting catastrophic forgetting.'],
  class_prompt: ['类别提示词（如 photo of a person），用于生成正则图的 caption。建议与实例词构成「类别 + 触发词」对照。', 'Class caption (e.g. photo of a person) for regularization images; contrasts with the instance trigger.'],
  instance_prompt: ['实例提示词：训练图的主 caption 模板，通常含触发词。建议所有概念统一格式，触发词放首位。', 'Instance caption template for training images, usually containing the trigger word first and kept uniform across concepts.'],
  class_tokens: ['类别 token（DreamBooth 风格）。建议与 class_prompt 保持一致语义。', 'Class tokens in DreamBooth style; keep semantics consistent with the class prompt.'],
  num_class_images: ['为类别提示词预生成的图像数量（0=不生成）。推荐范围：100–200 与训练图同量级；运行时默认 100。', 'Number of generated class images (0 = none). Recommended: 100-200, same scale as training images; runtime default 100.'],
  prior_loss_weight: ['先验损失权重：正则项相对主损失的比例。推荐范围：1.0（默认）或 0.5–1；仅提供了正则集时生效。', 'Weight of the prior-preservation term relative to main loss. Recommended: 1.0 default or 0.5-1; needs a regularization set.'],

  // ── 分辨率/分桶 ──
  resolution: [{ zh: '训练分辨率「宽,高」，须为 64 的倍数，匹配底模训练分辨率最佳。推荐范围：SDXL/Flux/Anima 1024,1024；SD1.5 512,512。降分辨率省显存但丢细节。', en: 'Training resolution "width,height", multiples of 64. Match your base model: 1024,1024 for SDXL/Flux/Anima, 512,512 for SD1.5.' }],
  enable_bucket: ['宽高比分桶（ARB）：把不同比例的图分进各桶减少裁剪。UNet 路线全支持；DiT cache-first 族主要影响 online/重建路径。建议保持开启（默认 true）。', 'Aspect-ratio bucketing groups images by ratio to reduce cropping. Fully supported on UNet routes; on DiT cache-first families it mainly affects online/rebuild paths. Keep on (default).'],
  bucket_no_upscale: ['桶内不放大小于目标分辨率的图（只缩大图）。建议小图多时开启避免放大模糊；追求统一尺寸时关闭。', 'Never upscale smaller images to fit a bucket (only downscale larger). Enable with many small images to avoid blur from upsampling.'],
  bucket_reso_steps: ['桶分辨率的划分步进（px）。推荐范围：64（标准）；低显存模式可 32；DiT 路线见 enable_bucket 说明。', 'Bucket resolution granularity in px. Recommended: 64 standard; 32 in low-VRAM profiles.'],
  bucket_selection_mode: ['分桶策略：aspect 宽高比匹配（默认）；area 面积匹配；no_downscale/no_upscale 为旧行为别名（行为等价规范名）。建议保持 aspect。', 'Bucketing strategy: aspect (default), area-based, or legacy aliases equivalent to their canonical names. Keep aspect.'],
  min_bucket_reso: ['桶允许的最小边长，过小会产生极端拉伸样本。推荐范围：256 以上且不超过 resolution 一半太多。', 'Smallest allowed bucket edge. Use >=256 and not far below half of resolution to avoid extreme stretching.'],
  max_bucket_reso: ['桶允许的最大边长；cache-first 回放通常沿用构建时分辨率。推荐范围：不超过 resolution 的 2 倍。', 'Largest allowed bucket edge; at most ~2x resolution. Cache-first replay typically reuses build-time resolution.'],

  // ── Caption ──
  caption_extension: ['标注文件扩展名（默认 .txt，与图片同名）。建议全库统一一种扩展名，混用会导致部分图读不到标注。', 'Caption file extension (.txt by default, same basename as image). Standardize on one or some images will silently lose captions.'],
  shuffle_caption: ['每次读取时随机打乱逗号 tag 顺序，防止模型依赖固定位置。建议几乎所有逗号分隔 tag 训练开启（社区惯例）。', 'Shuffles comma-separated tags each read so the model never relies on positions. Enable for nearly all tag-style captions (community standard).'],
  keep_tokens: ['打乱 tag 时保持前 N 个不动（通常固定触发词）。推荐范围：有 1 个触发词设 1；多触发词设对应数量；0 不保护。', 'Keeps the first N tags fixed during shuffling (usually trigger words). Set to your trigger count; 0 protects none.'],
  keep_tokens_separator: ['受保护前缀的自定义分隔符（如 |||）。建议仅当用特殊前缀结构时填写，留空按逗号处理。', 'Custom separator delimiting the protected prefix (e.g. |||). Leave empty to use commas.'],
  caption_tag_dropout_rate: ['按 tag 维度随机丢弃的概率，提升鲁棒性。推荐范围：0–0.05；触发词会被 keep_tokens/保护规则豁免。', 'Per-tag random dropout probability for robustness. Recommended: 0-0.05; triggers stay exempt via keep_tokens rules.'],
  caption_tag_dropout_target_mode: ['命中保护外 tag 后的处理方式：drop_all 全部移除；random_n 只随机移除 N 个。建议 drop_all（默认）。', 'How matched non-protected tags are handled: drop all, or randomly drop N. Keep drop_all.'],
  caption_tag_dropout_target_count: ['random_n 模式下每张图随机丢弃的命中 tag 数。推荐范围：1（默认）。', 'Tags dropped per image in random_n mode. Recommended: 1.'],
  caption_tag_mutate_scope: ['tag 变动范围：all 全部可动；after_separator 仅分隔符之后可动（头部前缀始终保留）。建议有结构化前缀时选 after_separator。', 'Which tags may change: all, or only after the separator (protected head prefix). Choose after_separator when using structured prefixes.'],
  caption_dropout_rate: ['整句 caption 以该概率完全置空（无条件学习）。推荐范围：0–0.05；CFG 相关训练才需要。', 'Probability the whole caption is dropped (unconditional learning). Recommended: 0-0.05; mainly for CFG-related training.'],
  caption_dropout_every_n_epochs: ['每 N 个 epoch 每 N 个 epoch 整批丢弃全部 caption 一个 epoch。推荐范围：保持 0；现代流程改用 caption_dropout_rate。', 'Drops all captions for one epoch every N epochs. Prefer rate-based dropout; keep 0 here.'],
  caption_replacements: ['caption 替换规则表（原文→新文），启用正则后按正则匹配。建议清洗统一术语时使用，规则勿过多。', 'Caption rewrite table (old->new), regex-aware once enabled. Use sparingly for term normalization.'],
  caption_replacements_regex: ['替换规则用正则而非字面量匹配。建议规则确实需要模式匹配时开启。', 'Interpret replacement rules as regex instead of literals. Enable only for pattern-based rules.'],
  caption_protect_prefix_from_dropout: ['让 dropout/shuffle 不触碰分隔符之前的前缀段。建议使用「前缀|||特征」结构时开启。', 'Shield the prefix before the separator from dropout/shuffle. Enable for prefix|||body caption layouts.'],
  caption_variants_enabled: ['多 caption 变体训练：同一图按变体文件轮换不同描述。建议需要一图多描述泛化时开启。', 'Rotate multiple caption variants per image for paraphrase generalization.'],
  caption_variant_ratio: ['非主变体的采样占比。推荐范围：0.2–0.5。', 'Sampling share of non-primary variants. Recommended: 0.2-0.5.'],
  caption_variant_schedule: ['变体切换调度（alternate 轮换等）。建议 alternate。', 'Variant rotation schedule. alternate recommended.'],
  caption_variant_loss_adaptive: ['按各变体 loss 动态调整采样比例。建议实验性开启，先跑固定比例基线。', 'Adapt variant sampling by per-variant loss. Experimental; baseline fixed ratios first.'],
  dual_caption_enabled: ['从结构化标注读取 short/long 两路 caption 分别训练。建议两套描述都高质量时开启。', 'Read short+long caption channels from structured annotations. Enable when both are high quality.'],
  caption_source_mix_enabled: ['Anima/Newbie 结构化 JSON caption 的 Tag/NL 混合采样开关。建议默认关闭，想混合触发词与自然语言时开。', 'Mixed Tag/NL sampling over structured JSON captions (Anima/Newbie). Off by default; on to blend triggers with natural language.'],
  caption_source_trigger_only_ratio: ['仅输出触发词（不带任何描述）的采样权重 %。推荐范围：10（默认）；四路比例之和应为 100。', 'Share (%) of samples that emit only the trigger word. Recommended: 10; ratios should sum to 100.'],
  caption_source_tag_ratio: ['输出「触发词 + Tag」的采样权重 %。推荐范围：20（默认）。', 'Share (%) emitting trigger + tags. Recommended: 20.'],
  caption_source_nl_ratio: ['输出「触发词 + 自然语言」的采样权重 %。推荐范围：65（默认，NL 为主利于泛化）。', 'Share (%) emitting trigger + natural language. Recommended: 65 (NL-heavy generalizes better).'],
  caption_source_empty_ratio: ['完全空文本采样占比 %。推荐范围：5（默认，保留无条件能力）；过高伤对齐。', 'Share (%) of empty-text samples. Recommended: 5; too high hurts alignment.'],
  nl_dropout_rate: ['对结构化 JSON 中 NL 描述的随机丢弃概率。推荐范围：0–0.1。', 'Dropout probability for NL descriptions in structured JSON. Recommended: 0-0.1.'],
  shuffle_caption_tags_only: ['结构化 JSON 标注只打乱 tags 部分，自然语言句子保持原序。建议 JSON 双通道标注时开启。', 'Shuffle only the tags array of structured JSON while keeping NL sentences intact. Enable for dual-channel JSON labels.'],
  sfad_enabled: ['SFAD 按标签频率调节 dropout：高频 tag 多丢、稀有 tag 保护。建议标签频次极不均时开启。', 'SFAD scales tag dropout by frequency: common tags dropped more, rare ones protected. Helps skewed tag distributions.'],
  sfad_drop_strength: ['频率→丢弃率的指数强度，0 近似均匀。建议 1（默认）。', 'Exponent mapping frequency to drop rate; 0 is near-uniform. Keep 1.'],
  sfad_trigger_protect: ['受保护触发词的丢弃率下限。推荐范围：0–0.3；0 表示永不丢触发词。', 'Floor of drop rate applied to protected triggers. Recommended: 0 (never drop) up to 0.3.'],
  sfad_warmup_steps: ['保护强度缓入步数，0 立即生效。建议 0。', 'Ramp-in steps for protection strength; 0 applies immediately.'],
  random_triggers_position: ['触发词注入到 caption 的位置（prepend 前置等）。建议 prepend（默认）保证触发词在首位。', 'Where injected trigger words land in the caption. prepend keeps them first (recommended).'],
  random_triggers_probability: ['注入随机触发词的概率。推荐范围：1.0 总是注入；0 关闭。', 'Probability of injecting a random trigger. 1.0 always; 0 disables.'],
  max_token_length: ['文本 token 截断上限。CLIP×3=225 为常用值（75 的倍数），后端最小 75。推荐范围：225 或更短以聚焦主体。', 'Token truncation cap for text encoders; 225 (=75x3) is common, minimum 75. Shorter focuses on the subject.'],
  qwen3_max_token_length: ['Qwen3 编码 token 上限。推荐范围：512（默认）以内，更长更慢更占缓存。', 'Max Qwen3 tokens. Recommended: within default 512; longer costs speed/cache.'],
  t5_max_token_length: ['T5 编码 token 上限。推荐范围：512（默认）；长描述任务可到 768 但缓存翻倍。', 'Max T5 tokens. Recommended: 512; up to 768 for long captions at double cache cost.'],
  t5xxl_max_token_length: ['T5-XXL 编码 token 上限。推荐范围：256–512；显存紧张取低值。', 'Max T5-XXL tokens. Recommended: 256-512; lower when VRAM-bound.'],
  newbie_clip_max_token_length: ['Newbie CLIP 通道 token 上限。推荐范围：2048 内按需；过大拖慢缓存构建。', 'Token cap for the Newbie CLIP channel. Stay within 2048 as needed.'],
  newbie_gemma_max_token_length: ['Newbie Gemma 通道 token 上限。推荐范围：512（默认）。', 'Token cap for the Newbie Gemma channel. Recommended: 512.'],
  newbie_caption_length_bucket_size: ['按 caption 长度分桶的大小，减少 padding 浪费。推荐范围：0 自动或 32–128。', 'Caption-length bucket size to cut padding waste. 0 auto or 32-128.'],
  anima_text_token_limit: ['在线路径的 Qwen3/T5 截断长度，>0 仅用于冒烟调试。推荐范围：0（不截断）。', 'Online-path truncation for Qwen3/T5; 0 disables. Nonzero values are smoke/debug aids only.'],
  anima_cached_text_token_limit: ['缓存路径的截断长度，0 完整缓存。推荐范围：0（保持完整）。', 'Cached-path truncation; 0 caches full length. Keep 0.'],
  anima_fixed_text_tokens: ['固定文本 token 长度便于静态 shape。建议 0 动态；compile 静态路线再考虑。', 'Pin text token length for static shapes; 0 keeps dynamic. Relevant only for static compile routes.'],
  anima_fixed_qwen3_tokens: ['仅约束 Qwen3 hidden 长度，0 动态。建议 0。', 'Pin Qwen3 hidden length; 0 dynamic.'],
  anima_fixed_t5_tokens: ['仅约束 T5 token ids 长度，0 动态。建议 0。', 'Pin T5 id length; 0 dynamic.'],
  anima_fixed_visual_tokens: ['固定视觉 token 数，0 保持缓存大小。建议 0。', 'Pin visual token count; 0 keeps cached sizes.'],

  // ── 数据增强 ──
  flip_aug: ['随机水平翻转增强，双倍扩充视角。建议对称题材（人物/物体）开启；文字/方向敏感内容关闭。', 'Random horizontal flip doubles viewpoints. Great for symmetric subjects; off for text/direction-sensitive content.'],
  color_aug: ['随机颜色扰动增强。建议与 cache_latents_to_disk 互斥考量（颜色增强不能磁盘缓存）；风格色敏感时关闭。', 'Random color jitter augmentation. Incompatible with disk latent caching; avoid for palette-critical styles.'],
  random_crop: ['随机裁剪数据增强。建议构图多样性需求时开；主体易被裁掉时关。', 'Random crop augmentation for composition variety; disable if it crops out subjects.'],
  albumentations_enabled: ['启用自定义 albumentations 增强 JSON 管道。建议熟悉该库后再用，配置错误会中断训练。', 'Enable a custom albumentations pipeline defined as JSON. For users familiar with the library; bad configs break runs.'],
  albumentations_pipeline: ['albumentations 增强 JSON 定义。建议从官方示例抄起，逐个增强验证。', 'The albumentations pipeline JSON. Start from official examples and validate stepwise.'],
  adaptive_step_logging_enabled: ['进度/TensorBoard 写入耗时过高时自动降低日志频率。建议大数据集长训保持开启（默认 true）。', 'Auto-throttles progress/log writes when they dominate step time. Keep on for long runs (default).'],
  adaptive_step_logging_threshold: ['日志耗时占单步总耗时的阈值比例。推荐范围：0.01（1%）附近。', 'Log-time share of step time above which throttling kicks in. Recommended: ~0.01.'],

  // ── 缓存 ──
  cache_latents: ['缓存 VAE latent 避免每步重复编码，大幅提速。建议除在线增强需求外保持开启（默认 true）。', 'Caches VAE latents to skip re-encoding every step: major speedup. Keep on unless you need online pixel-space augmentation.'],
  cache_latents_to_disk: ['latent 持久化到磁盘跨 run 复用（首次慢后续快）。建议数据集稳定且磁盘够快时开启；开了就不能用颜色类在线增强。', 'Persists latents to disk across runs: slower first pass, faster after. Enable for stable datasets on fast disks; incompatible with color aug.'],
  cache_text_encoder_outputs: ['缓存文本编码器输出，显著省显存——但 TE 不再参与训练。建议 TE 冻结场景开启；要训 TE 必须关闭。', 'Caches TE outputs to save VRAM but freezes the text encoder entirely. On for frozen TE; must be off to train TE.'],
  cache_text_encoder_outputs_to_disk: ['TE 输出持久化到磁盘跨 run 复用。建议 caption 固定时开启，改动 caption 需重建缓存。', 'Persists TE outputs to disk across runs. Good for fixed captions; rebuild the cache after edits.'],
  latent_cache_disk_format: ['latent 磁盘缓存容器格式（npz 等）。建议 npz 兼容性最好。', 'Container format for disk latent cache. npz has the widest compatibility.'],
  latent_cache_disk_dtype: ['latent 落盘精度。fp16 体积减半且质量损失可忽略。建议 fp16。', 'On-disk dtype for latents. fp16 halves size with negligible quality loss.'],
  text_encoder_outputs_cache_disk_format: ['TE 输出磁盘缓存格式。建议 npz。', 'Container format for the TE-output disk cache. npz recommended.'],
  text_encoder_outputs_cache_disk_dtype: ['TE 输出落盘精度。建议 float16 平衡体积与质量。', 'On-disk dtype for TE outputs. float16 balances size and fidelity.'],
  text_encoder_outputs_cache_dtype: ['内存中 TE 缓存精度。建议 fp16；数值异常时回 float32。', 'In-memory dtype for cached TE outputs. fp16 recommended; float32 if artifacts appear.'],
  text_encoder_batch_size: ['TE 缓存编码批量。推荐范围：1–8，显存紧张减半。', 'Batch size for TE cache encoding. Recommended: 1-8, halve when VRAM-bound.'],
  vae_batch_size: ['VAE 缓存编码批量；0 自动。推荐范围：4–16，OOM 时减半。', 'Batch size for VAE cache encoding; 0 auto. Recommended: 4-16, halve on OOM.'],
  use_cache: [
    { zh: '读取/写入 *_zimage.npz latent 与文本条件缓存（带变体自描述校验），避免训练时常驻 VAE/Qwen3 编码器；关闭则回落 live 编码。推荐范围：保持 true（默认）；数据变更后强制重建一次。', en: 'Reads/writes *_zimage.npz latent and text-conditioning caches (self-describing validation) instead of keeping VAE/Qwen3 resident; off falls back to live encoding. Keep true (default); force one rebuild after data changes.', only: '_zimage.npz' },
    { zh: '默认读取兼容的 *_wan22.npz latent/文本条件缓存，避免常驻 VAE/文本编码器。推荐范围：保持 true（默认）。', en: 'Reads compatible *_wan22.npz latent/text caches to avoid resident encoders. Keep true (default).', only: '_wan22' },
    { zh: '启用后优先读 latent/TE 缓存（*_flux2 格式）。推荐范围：保持开启；缓存与模型版本必须匹配。', en: 'Prefers *_flux2 latent/TE caches when present. Keep on; cache and model versions must match.', only: '_flux2' },
    { zh: '推荐开启：读 latent/指令 TE 缓存（Qwen-Image 格式）。数据变化后需重建。', en: 'Recommended: reuse latent/instruction TE caches (Qwen-Image format); rebuild after data changes.', only: '指令' },
    { zh: 'Edit 缓存额外包含 ref_latents 参考图分支。推荐范围：编辑类任务保持 true。', en: 'Edit caches additionally carry ref_latents for reference images. Keep true for edit tasks.', only: 'ref_latents' },
    { zh: '优先复用 Krea-2 latent 与文本编码缓存，避免训练时常驻 VAE/文本编码器。推荐范围：保持 true（默认）。', en: 'Reuses Krea-2 latent and TE caches to avoid resident encoders during training. Keep true (default).', only: 'Krea' },
    { zh: '优先复用已生成的 latent 与文本条件缓存，避免常驻 VAE/Qwen3 编码器。推荐范围：数据未变时保持 true（默认）；改动数据集/caption 后需重建。', en: 'Reuse previously built latent/text-conditioning caches instead of keeping VAE/Qwen3 resident. Keep true (default) while data is unchanged; rebuild after dataset/caption edits.' },
  ],
  clear_dataset_npz_before_train: ['worker 训练前清空旧 .npz 缓存强制重建。建议仅在数据集大改后手动开一次。', 'Workers wipe stale .npz caches before training to force a rebuild. Toggle once after big dataset changes.'],
  newbie_force_cache_only: ['只构建/补全缓存后提前退出，不进入训练循环；用于大数据集预处理。与「强制重建缓存」同开时仅重建并跳过训练。建议大批量预处理时单独跑一次。', 'Builds/completes the cache and exits early without entering the training loop; intended for pre-processing large datasets. Combined with force-rebuild-cache, it only rebuilds the cache and skips training.'],
  newbie_rebuild_cache: ['无视既有缓存强制重建全部工件。建议数据/标注变更后开一次，平时关闭。', 'Ignores existing caches and rebuilds everything. Run once after data/label changes.'],
  h3_cache_build_enabled: ['关闭则跳过缓存构建（要求缓存已存在）。建议首跑保持开启。', 'Skip cache building when off (requires existing cache). Keep on for first runs.'],
  h3_cache_include_audio: ['缓存构建同时编码音频 VAE 分支。音频任务建议开启。', 'Also encode the audio VAE branch during cache build. Enable for audio tasks.'],
  h3_cache_rebuild: ['忽略既有缓存工件并强制重建。建议数据变更后开一次，平时保持关闭。', 'Ignore existing cache artifacts and rebuild. Run once after data changes.'],
  h3_cache_max_pixels: ['单样本进入缓存的像素上限。推荐范围：262144（≈512x512，默认）。', 'Pixel cap per sample entering the cache. Recommended: 262144 (~512x512).'],
  h3_cache_max_samples: ['最多缓存的样本数；调试期可给小值快速迭代。推荐范围：0（不限制）。', 'Cap on cached sample count; 0 unlimited. Small values help debugging loops.'],
  cached_dataloader_auto_policy: ['缓存路线自动调 workers/prefetch 参数。建议保持开启（默认 true），手工调参被其覆盖。', 'Auto-tunes workers/prefetch on cached routes, overriding manual values. Keep on (default).'],
  dataloader_num_workers: ['DataLoader 工作进程数，影响取数吞吐。推荐范围：2–8；Windows 下过高会拖慢启动。', 'DataLoader worker processes feeding the GPU. Recommended: 2-8; very high values slow startup on Windows.'],
  num_workers: ['DataLoader worker 数（简写入口）。推荐范围：2–8，0 主进程加载。', 'DataLoader workers (alias entry). Recommended: 2-8; 0 loads in-process.'],
  prefetch_factor: ['每个 worker 预取的 batch 数。推荐范围：2（默认）即可，IO 慢可升到 4。', 'Batches each worker prefetches ahead. Recommended: 2; 4 on slow IO.'],
  persistent_data_loader_workers: ['epoch 间保留 worker 进程避免重启开销。建议数据集较大时保持开启（默认 true）。', 'Keeps workers alive between epochs to skip respawn cost. Keep on for larger datasets (default).'],
  dataset_intelligence_enabled: ['离线统一分析质量/Caption/难度/区域覆盖与概念稀有度并生成 Manifest，训练期只读 Manifest 不再加载检测模型。建议正式训练前开启做一次体检。', 'Offline analysis builds a Manifest of quality/caption/difficulty/coverage/rarity; training reads only the Manifest. Run it before serious training.'],
  dataset_intelligence_sampling_mode: ['Manifest 驱动的采样策略（fixed 顺序 / 课程等）。建议 fixed 起步，验证 Manifest 质量后再切课程。', 'Manifest-driven sampling strategy (fixed order vs curriculum). Start fixed; switch to curriculum after vetting the Manifest.'],
  dataset_intelligence_seed: ['采样计划种子：相同 Manifest+epoch+seed 得到相同计划。建议固定以便复现。', 'Seed for the sampling plan so identical inputs reproduce identical schedules. Fix it.'],
  dataset_intelligence_concept_rarity_strength: ['按概念稀有度平衡采样权重，缓解高频概念淹没稀有概念。推荐范围：0 关闭；0.5–2 渐进试探。', 'Balances sampling by concept rarity against dominance by frequent concepts. Recommended: probe 0.5-2; 0 off.'],
  dataset_intelligence_region_balance_strength: ['按区域覆盖率平衡采样，缓解背景区主导。推荐范围：0 关闭；0.5–2 试探。', 'Balances sampling by region coverage so backgrounds do not dominate. Recommended: probe 0.5-2; 0 off.'],
  dataset_intelligence_target_caption_language: ['Manifest caption 的目标语言（auto 跟随数据）。建议 auto；统一语言评测时显式指定。', 'Target language for Manifest captions; auto follows the data. Pin it only when evaluating in one language.'],
  dataset_intelligence_curriculum_easy_end: ['课程式训练：进度达到该比例前偏向简单样本。推荐范围：0.33（默认）。', 'Curriculum: prefer easy samples until this progress fraction. Recommended: ~0.33.'],
  dataset_intelligence_curriculum_normal_end: ['达到该比例后逐步开放困难样本；不得小于简单阶段终点。推荐范围：0.66（默认）。', 'After this fraction hard samples phase in; must exceed the easy-phase end. Recommended: ~0.66.'],
  dataset_intelligence_min_weight: ['样本权重下限（防过度抑制难例）。推荐范围：0.25（默认）。', 'Floor on sample weights preventing over-suppression. Recommended: 0.25.'],
  dataset_intelligence_max_weight: ['样本权重上限（防难例主导）。推荐范围：4（默认）。', 'Ceiling on sample weights preventing hard-sample domination. Recommended: 4.'],
  sample_difficulty_weighting_enabled: ['用数据集难度权重或 batch loss 自动强调难/简单样本。建议默认关闭，确认权重来源可靠后开。', 'Emphasize hard/easy samples via dataset weights or detached batch loss. Off by default; enable once weight sources are trusted.'],
  sample_difficulty_weighting_mode: ['难度来源策略（provided 用数据集自带 / auto 按 loss）。建议 provided 优先。', 'Difficulty source: provided dataset weights vs automatic loss-derived. Prefer provided.'],
  sample_difficulty_weighting_min: ['难度权重下限。推荐范围：0.25（默认）。', 'Lower clamp on difficulty weights. Recommended: 0.25.'],
  sample_difficulty_weighting_max: ['难度权重上限。推荐范围：4（默认）。', 'Upper clamp on difficulty weights. Recommended: 4.'],
  sample_difficulty_weighting_strength: ['难度加权强度：0 不改变，1 标准倾斜。推荐范围：组合多轴时从 0.25–1 开始。', 'Difficulty-weight strength: 0 identity, 1 standard tilt. Recommended: 0.25-1 when combined with other axes.'],
  timestep_weighting_enabled: ['按扩散时间步调整样本 loss，可与区域/噪声/难度权重相乘。建议明确想偏重结构或细节时开启。', 'Weights per-sample loss by diffusion timestep; multiplies with region/noise/difficulty axes. Use to bias structure vs detail deliberately.'],
  timestep_weighting_mode: ['时间步侧重方向（低噪偏细节/高噪偏结构；标定 LUT 离线自标定）。建议 uniform 起步。', 'Timestep tilt direction (low-noise detail vs high-noise structure; offline-calibrated LUT). Start uniform.'],
  timestep_weighting_strength: ['时间步权重强度：0 恒等，1 标准倾斜，上限 4。推荐范围：组合多轴时 0.25–1。', 'Timestep-weight strength: 0 identity, 1 standard tilt, cap 4. Recommended: 0.25-1 when combined.'],
  noise_weighting_mode: ['噪声侧重轴：低噪声偏细节、高噪声偏结构。建议 uniform 起步，观察出图短板再倾斜。', 'Noise-weight strength: 0 identity, 1 standard tilt, cap 4. Recommended: 0.25-1 when combined with other axes.'],
  noise_weighting_strength: ['噪声权重强度：0 不改变，1 标准倾斜，上限 4。推荐范围：组合多轴时 0.25–1。', 'Noise-weight strength: 0 identity, 1 standard tilt, cap 4.'],
  progressive_training_enabled: ['按优化进度切换训练阶段（阶段 LR/LoRA 模块/课程策略）。建议长周期精调项目使用，短训收益有限。', 'Switch training stages by optimizer progress (per-stage LR/modules/curriculum). Best for long fine-tuning projects.'],
  progressive_curriculum_seed: ['阶段内课程难度选择种子，相同 seed 可复现。建议固定 42（默认）即可。', 'Seed for in-stage curriculum picks; keep 42 for reproducibility.'],
};
